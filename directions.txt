sudo apt update
sudo apt upgrade

sudo apt install python3-pip

sudo pip3 install flask adafruit-circuitpython-neopixel

sudo python3 main.py


might want to set this to auto-run with pm2 while we're at it, then you can do pm2 save to daemonize this